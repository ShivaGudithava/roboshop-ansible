#echo -e "\e[36m>>>>>>>>>>> Insatll Mongo Client <<<<<<<<<<<<\e[0m"
#yum install mongodb-org-shell -y&>>/tmp/roboshop.log
#func_exit_status
#
#echo -e "\e[36m>>>>>>>>>>> Load user Schema <<<<<<<<<<<<\e[0m"
#mongo --host mongodb.gudishivadevops.online </app/schema/${component}.js&>>/tmp/roboshop.log
#func_exit_status

- name: Copy MongoDB Yum repo file
  ansible.builtin.copy:
    src: mongo.repo
    dest: /etc/yum.repos.d/mongo.repo

- name: Install MongoDB Client
  ansible.builtin.yum:
    name: mongodb-org-shell
    state: installed

- name: Load Schema
  ansible.builtin.shell: mongo --host mongodb-dev.gudishivadevops.online </app/schema/${component}.js
